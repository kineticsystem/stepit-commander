<?xml version="1.0"?>
<?xml-model href="http://download.ros.org/schema/package_format3.xsd" schematypens="http://www.w3.org/2001/XMLSchema"?>
<package format="3">
  <name>stepit_objectives</name>
  <version>0.1.0</version>
  <description>
    Objectives (BehaviorTree.CPP XML files) that the commander action server can execute.
    This package contains no code: it only ships the XML trees, so that the
    description of *what* the robot does is kept separate from *how* it is done.
  </description>
  <maintainer email="lentigrams@gmail.com">developer</maintainer>
  <license>MIT</license>

  <buildtool_depend>ament_cmake</buildtool_depend>

  <!-- Runtime only: the trees are loaded by stepit_server. -->
  <exec_depend>stepit_behaviors</exec_depend>

  <export>
    <build_type>ament_cmake</build_type>
  </export>
</package>
